public class vigenere {
	private String message;
	private String key;
	
	public vigenere(String message, String key) {
		this.message = message.toUpperCase();
	    this.key = key;
	}
	
	/*
  prerequisite: A message (to be decoded) and key must be inputed
  postrequisite: No alterations to the ciphertext or key, returns a String with the decoded cipherin uppercase
  */
	public String decoder() {
		//turn key to ascii, subtract values by 65
    //subtract values accordingly to encode the message
    //return decoded message
		
		return "";
	}
}
